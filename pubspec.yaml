name: flutter_lifecycle_aware
description: a lifecycle system on the flutter
version: 0.0.2
homepage: https://github.com/RuffianZhong/flutter_lifecycle


environment:
  sdk: ">=2.17.5 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

