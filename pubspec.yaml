name: flutter_lifecycle_aware
description: A library that make flutter to have a lifecycle like android or iOS. It can sense the lifecycle wherever you need them.
version: 0.0.3
homepage: https://github.com/RuffianZhong/flutter_lifecycle


environment:
  sdk: ">=2.17.5 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

